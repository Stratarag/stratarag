# stratarag (name reservation)

StrataRAG — agents that remember, RAG in every shape — is currently a
**Python** library: `pip install stratarag`.

This npm package reserves the name for a future TypeScript SDK.
Project home: https://github.com/stratarag-ai/stratarag
